import 'package:flutter/material.dart';

class SecondNotificationsScreen extends StatelessWidget {
  const SecondNotificationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Container(
        
      ),
    );
  }
}